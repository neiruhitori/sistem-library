APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:+zxZLI275M9z/rOd6pCeU0eGdhFBTqp9BjV4zZm+7B0=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=sistem_kasir
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=akunmasadepan14@gmail.com
MAIL_PASSWORD=hvcfgvxnfjvokqyw
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="akunmasadepan14@gmail.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:+zxZLI275M9z/rOd6pCeU0eGdhFBTqp9BjV4zZm+7B0=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=sistem_perpus
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=akunmasadepan14@gmail.com
MAIL_PASSWORD=hvcfgvxnfjvokqyw
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="akunmasadepan14@gmail.com"
MAIL_FROM_NAME="${APP_NAME}"

# CARA MEMBUAT LOGIN DENGAN GOOGLE DI LARAVEL
# langkah pertama composer require laravel/socialite

# di config/services.php tambahkan kode berikut
# 'google' => [
#     'client_id' => env('GOOGLE_CLIENT_ID'),
#     'client_secret' => env('GOOGLE_CLIENT_SECRET'),
#     'redirect' => env('GOOGLE_REDIRECT_URI'),
# ],

# di GCP pada Project Login laravel lalu di menu API & Services,
# di menu Library enable Google People API, setelah itu ke menu credentials,
# create credentials OAuth client ID, pilih Web application,
# lalu isi Authorized JavaScript origins dengan http://localhost:8000, Authorized redirect URIs dengan http://localhost:8000/auth/google/callback,
# setelah itu akan muncul Client ID dan Client Secret, copy paste ke bawah ini

# tambahkan kode ini di web.php/auth.php:
# Route::get('/auth/google', function () {
#         return Socialite::driver('google')->redirect();
#     })->middleware('guest')->name('auth.google');

#     Route::get('/auth/google/callback', function () {
#         $googleUser = Socialite::driver('google')->stateless()->user();

#         $user = User::updateOrCreate(
#             ['email' => $googleUser->getEmail()],
#             [
#                 'name' => $googleUser->getName(),
#                 'email_verified_at' => now(),
#                 'password' => bcrypt(Str::random(16)),
#             ]
#         );

#         Auth::login($user);

#         return redirect()->intended('/dashboard');
#     });
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
